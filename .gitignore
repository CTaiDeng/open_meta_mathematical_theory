# Encoding/EOL policy: UTF-8 (no BOM) + LF
# Enforced via `.gitattributes` and `.githooks/pre-commit.ps1`
# Details: see `AGENTS.md`
res
out
.venv
.pip-cache
.idea
src\full_reference
src\docs
# Python bytecode
*.pyc

NoteBook
src\app_docs